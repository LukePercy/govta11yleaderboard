# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
venv/
.venv/
env/
.env/

# Data pipeline output — committed so the app works without running process_data.py.
# Uncomment the line below to exclude it and require contributors to generate locally.
# data/leaderboard.json

# Raw quarterly scan folders — excluded to avoid secret-scanning false positives on CSV
# content and GitHub's 100 MB file-size limit. Use the companion .zip archives instead.
# Unzip locally and run process_data.py to regenerate data/leaderboard.json.
*_quarterly_cwac_scan/

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~
